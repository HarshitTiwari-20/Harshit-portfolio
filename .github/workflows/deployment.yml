name: Next.js CI/CD

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install Dependencies
        run: npm ci

      

      - name: Build Next.js App
        run: npm run build

  # Example deployment job (Uncomment and configure based on your hosting provider)
  # deploy:
  #   needs: build-and-test
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - name: Checkout Repository
  #       uses: actions/checkout@v4
  #       
  #     - name: Deploy to Vercel
  #       uses: amondnet/vercel-action@v20
  #       with:
  #         vercel-token: ${{ secrets.VERCEL_TOKEN }} # Required in GitHub Secrets
  #         vercel-org-id: ${{ secrets.VERCEL_ORG_ID }} # Required in GitHub Secrets
  #         vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }} # Required in GitHub Secrets
  #         vercel-args: '--prod'
